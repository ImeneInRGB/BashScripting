# BashScripting
A collection of bash scripts I wrote
creation_script.sh : create a bunch of files and directories.\n
reorganization_script.sh : organize and move the files (depending on their name and date/time of creation) into subdirectories using regex pattern.

